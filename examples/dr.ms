(define-record-type test-record
                    (make-test-record x)
                    test-record?
                    (x test-record-x)
                    (y test-record-y set-test-record-y!))
    
;;; MOSVM2
;;; MOSVM2
